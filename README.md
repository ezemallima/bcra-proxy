# BCRA Proxy — Piattelli Créditos

Proxy intermediario para consultar la API del BCRA sin restricciones de CORS.

## Endpoints
- GET /deudas/{cuit} — situación actual
- GET /deudas/{cuit}/historial — historial completo
- GET /health — estado del servidor
