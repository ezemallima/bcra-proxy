flask>=3.0,<4.0
flask-cors>=4.0,<5.0
requests>=2.31,<3.0
urllib3>=2.0,<3.0
openpyxl>=3.1,<4.0
PyMuPDF>=1.24,<2.0
gunicorn>=22.0,<24.0
boto3>=1.34,<2.0
botocore>=1.34,<2.0
gdown>=5.1,<6.0

# ARCA — firma CMS/PKCS#7 del TRA y lectura del certificado X.509 (arca_ws.py).
# >=42 es obligatorio: usamos Certificate.not_valid_after_utc, agregado en 42.0.
# No se requiere zeep ni pyOpenSSL: los envelopes SOAP se arman con
# xml.etree (stdlib), evitando esas dependencias pesadas.
cryptography>=42.0,<46.0
